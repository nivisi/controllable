name: controllable_generator
description: A generator for the controllable and controllable_flutter packages that makes working with controllable easier.
version: 0.0.1
homepage: null

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  build: null
  dart_code_writer: ^0.0.4
  flutter:
    sdk: flutter
  glob: ^2.0.1
  provider: ^6.0.2
  source_gen: null

dev_dependencies:
  build_runner: null
  build_test: null
  flutter_lints: ^1.0.0
